- def criar_token(id_usuario,duracao_token=timedelta(minutes=ACCESS_TOKEN_EXPIRE_MINUTES)):
    data_expiracao = datetime.now(timezone.utc) + duracao_token
    dic_informacoes = {"sub": str(id_usuario),"exp": data_expiracao}
    jwt_token = jwt.encode(dic_informacoes, SECRET_KEY,ALGORITHM)
    token = f"awifs2jde23j2dka{id_usuario}"
    return jwt_token

    - "def criar_token(id_usuario,duracao_token=timedelta(minutes=ACCESS_TOKEN_EXPIRE_MINUTES))":
            - def criar_token: created an function called "criar_token"
            - id_usuario: refers to datas inside in column "id_usuario" in sql database.
            - duracao_token=timedelta(minutes=ACCESS_TOKEN_EXPIRE_MINUTES): defines the time during which the token is valid 
                - timedelta: is Python’s way of representing time intervals. It lets you add, subtract, 
                             and measure durations between  dates and times with precision down to microseconds. 